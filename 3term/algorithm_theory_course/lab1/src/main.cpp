#include <iostream>
#include <string>
#include <fstream>

int main() {

    std::string filename;
    int fileAmount;
    int maxCapacity;

    std::cin >> filename;
    std::cin >> fileAmount;
    std::cin >> maxCapacity;

}
